import { FC } from 'react'
import CustomizedTable from '../../components/CustomizedTable/CustomizedTable'

const List: FC = () => {
    return (
        <div>
            <CustomizedTable />
        </div>
    )
}

export default List